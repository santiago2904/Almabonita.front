import { ARCreator } from '@/components/ar/ARCreator';

export default function ARCreatorPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-purple-100">
      <ARCreator />
    </div>
  );
}